# Import Conflict Example

This folder contains a minimal import conflict example for grebase demos.

Note: Grebase does not use files in this folder at runtime. These are
illustrative examples only.

Files:
- `app.py` includes a merge conflict between imports.
- `expected.py` shows the resolved output.
