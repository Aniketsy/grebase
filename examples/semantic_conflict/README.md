# Semantic Conflict Example

This folder contains a semantic conflict example that requires a user decision.

Note: Grebase does not use files in this folder at runtime. These are
illustrative examples only.

Files:
- `settings.py` contains a conflict with different timeout values.
