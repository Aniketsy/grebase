# Lockfile Conflict Example

This folder contains a minimal lockfile conflict example for grebase demos.

Note: Grebase does not use files in this folder at runtime. These are
illustrative examples only.

Files:
- `poetry.lock` contains a conflict.
- `README.md` explains the expected regeneration flow.
